div.filterPanel(current-child="currentChild", filter-message="filterMessage", sections="sections", select="selectSection", current="currentSection", extra="extraParams", sort-types="sortTypes", params="queryParams")
div.filterView.contextMenu(current-child="currentChild", section="currentSection", query="queryItems", params="queryParams", ng-dblclick="uploadContent($event)")

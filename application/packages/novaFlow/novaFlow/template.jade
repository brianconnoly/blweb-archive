div.frameTabs
    //- div.novaFlowBoxTabs(ng-repeat="flowBox in flow.flowBoxes", ng-style="{'width':flowBox.width}")
    //-     div.novaFlowFrameTab.noDrag(ng-repeat="flowFrame in flowBox.flowFrames")
div.flowBoxes
    //- div.novaFlowBox(ng-repeat="flowBox in flow.flowBoxes")

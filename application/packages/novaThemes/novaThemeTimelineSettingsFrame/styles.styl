overflow-y auto

.addChannel
    line-height 30px
    cursor pointer
    float left

    span
        pointer-events none

    .plus
        font-size 25px
        float left
        margin 0 5px

.ruleItem
    overflow: hidden;
    padding: 4px 5px // 4px 5px;
    .textButton
        font-size 14px
        line-height 24px
        float right
        z-index 2

.themeChannel
    .novaRemove
        right 0
        top 0
        position absolute

    &:hover
        .novaRemove
            opacity 1
